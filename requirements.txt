# Pinned minimum versions — mirrors pyproject.toml [project.dependencies].
# For fully reproducible builds, run `pip install -e .` then
# `pip freeze > requirements.txt` to capture exact transitive versions.
anthropic>=0.40.0
rasterio>=1.3.0
geopandas>=0.14.0
pyproj>=3.6.0
duckdb>=0.10.0
pyarrow>=14.0.0
pandas>=2.0.0
pydantic>=2.0.0
folium>=0.15.0
numpy>=1.26.0
httpx>=0.27.0
python-dotenv>=1.0.0
pytest>=8.0.0
shapely>=2.0.0
tqdm>=4.66.0
matplotlib>=3.8.0
seaborn>=0.13.0
